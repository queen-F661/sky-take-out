# 2026-09-06

- 修复分类删除接口 bug（`CategoryServiceImpl.delete`）：
  - 原代码条件 `!(idx01 != null && idx02 != null)` 写反——count 查询结果永远非 null，导致删除逻辑永远不执行（点删除无效果）。
  - 改为：dish/setmeal 关联数 > 0 时抛 `DeletionNotAllowedException`（项目已有该异常类，继承 BaseException，会被全局异常处理器统一捕获返回提示），否则才执行 `categoryMapper.delete(id)`。
- 顺带确认：`DishMapper`/`SetmealMapper` 的 `countByCategoryId` SQL 正确；`CategoryController.delete(Long id)` 走 query 参数绑定没问题。
- 员工模块（EmployeeController）目前没有删除接口，用户问的"删除"实际是分类删除。
