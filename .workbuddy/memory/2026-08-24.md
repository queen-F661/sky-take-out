## 2026-08-24

- 创建 `.gitignore`（标准 Java/Maven 忽略规则：`**/target/`、`.idea`、`*.iml`、`*.class`、`*Test.java`、`**/test/`）。
- 【重要】修复 IntelliJ 编译报错 `java.nio.file.InvalidPathException: Illegal char <?>`：
  - 根因：父目录 `D:\project\cjwm\sky‑take‑out` 名称里的连字符是 **U+2011 不间断连字符**（非 ASCII `-`），JPS 无法解析。
  - 处理：目录被 WorkBuddy 自身进程占用无法重命名，改用**复制迁移**——项目现位于 `D:\project\cjwm\sky-take-out\sky-take-out`（纯 ASCII 路径），并修正了 `.idea/workspace.xml` 中的旧路径引用。
  - 待办：用户重启电脑/确认无占用后，手动删除旧目录 `D:\project\cjwm\sky‑take‑out`（含 U+2011 那个）。
- 项目结构：父 pom + sky-common / sky-pojo / sky-server 三个子模块；git remote：`git@github.com:queen-F661/sky-take-out.git`（分支 main）。
- 环境注意：Windows 下复制目录名若含 U+2011 等特殊 Unicode 连字符，会导致 Java/JetBrains 工具链报 Illegal char；以后创建项目路径务必用纯 ASCII。
